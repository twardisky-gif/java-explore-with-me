FROM eclipse-temurin:21-jre

WORKDIR /app

# The main service is implemented in stage two. This image deliberately contains
# no application logic and is not part of the stage-one Compose configuration.
CMD ["java", "-version"]
